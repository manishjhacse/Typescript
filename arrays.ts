const superHeroes: string[] = [];
const power: number[] = [];

superHeroes.push("IronMan");
power.push(100);

type User = {
  name: string;
  isActive: boolean;
};
const users: User[] = [];
users.push({ name: "manish", isActive: true });
console.log(users);
